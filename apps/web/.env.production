# Convex deployment URL for production builds.
# NOTE: prod currently points at the same deployment as dev. When a dedicated
# prod deployment exists, change this file only — .env.development stays as-is.
VITE_CONVEX_URL=https://warmhearted-quail-48.convex.cloud
