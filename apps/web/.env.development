# Convex deployment URL — shared dev deployment for this repo.
# Public value (inlined into the client bundle by Vite), safe to commit.
# Override locally with apps/web/.env.local if you use your own deployment.
VITE_CONVEX_URL=https://warmhearted-quail-48.convex.cloud
